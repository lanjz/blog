import{_ as n,o as s,c as a,e}from"./app-p5-cwF4Z.js";const t="/lanjz/assets/f_1-3fON9Gav.jpeg",p="/lanjz/assets/f_2-ZRgRSBuW.jpeg",o="/lanjz/assets/f_3-HGA06pW6.jpeg",i="/lanjz/assets/t_1-xEztXSBo.png",c={},r=e(`<h1 id="时间切片" tabindex="-1"><a class="header-anchor" href="#时间切片" aria-hidden="true">#</a> 时间切片</h1><p><strong>浏览器一帧都会干些什么？</strong></p><p>我们都知道，页面的内容都是一帧一帧绘制出来的，浏览器刷新率代表浏览器一秒绘制多少帧。原则上说 1s 内绘制的帧数也多，画面表现就也细腻。目前浏览器大多是 60Hz（60帧/s），每一帧耗时也就是在 16.6ms 左右。那么在这一帧的（16.6ms） 过程中浏览器又干了些什么呢？</p><p>浏览器一帧会经过下面这几个过程：</p><ol><li><p>接受输入事件</p></li><li><p>执行事件回调</p></li><li><p>开始一帧</p></li><li><p>执行 RAF (RequestAnimationFrame)</p></li><li><p>页面布局，样式计算</p></li><li><p>绘制渲染</p></li><li><p>执行 RIC (RequestIdelCallback)</p></li></ol><p>因此如果 React 调和算法在每次更新都遍历整个 App 树，然后渲染，如果遍历的时间超过 16ms, 就会导致丢帧，你知道丢帧并不太好，会导致动画卡顿，不连贯</p><p>这就是赋予更新任务的优先级非常好的一个大的原因，而不是盲目应用每次更新到调和器。另一个非常的特性是可以暂停，在下一帧恢复。这种方式 React 可以在 16ms 内更好地控制任务渲染的时间。</p><p>这导致了 React 团队重写了调和算法，新的算法被称为 Fiber</p><h3 id="requestidlecallback-的启示" tabindex="-1"><a class="header-anchor" href="#requestidlecallback-的启示" aria-hidden="true">#</a> requestIdleCallback 的启示</h3><p>上面第七步的 RIC 事件不是每一帧结束都会执行，只有在一帧的 16.6ms 中做完了前面 6 件事儿且还有剩余时间，才会执行。如果一帧执行结束后还有时间执行 RIC 事件，那么下一帧需要在事件执行结束才能继续渲染，所以 RIC 执行不要超过 30ms，如果长时间不将控制权交还给浏览器，会影响下一帧的渲染，导致页面出现卡顿和事件响应不及时</p><p>我们以浏览器是否有剩余时间作为任务中断的标准，那么我们需要一种机制，当浏览器有剩余时间时通知我们。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token comment">// 用法示例</span>
<span class="token keyword">var</span> tasksNum <span class="token operator">=</span> <span class="token number">10000</span>

<span class="token function">requestIdleCallback</span><span class="token punctuation">(</span>unImportWork<span class="token punctuation">)</span>

<span class="token keyword">function</span> <span class="token function">unImportWork</span><span class="token punctuation">(</span><span class="token parameter">deadline</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
 <span class="token keyword">while</span> <span class="token punctuation">(</span>deadline<span class="token punctuation">.</span><span class="token function">timeRemaining</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> tasksNum <span class="token operator">&gt;</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">执行了</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span><span class="token number">10000</span> <span class="token operator">-</span> tasksNum <span class="token operator">+</span> <span class="token number">1</span><span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">个任务</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">)</span>
  tasksNum<span class="token operator">--</span>
 <span class="token punctuation">}</span>
 <span class="token keyword">if</span> <span class="token punctuation">(</span>tasksNum <span class="token operator">&gt;</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token comment">// 在未来的帧中继续执行</span>
  <span class="token function">requestIdleCallback</span><span class="token punctuation">(</span>unImportWork<span class="token punctuation">)</span>
 <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>但是由于以下因素，Facebook 抛弃了 <code>requestIdleCallback</code> 的原生 API：</p><ul><li><p>浏览器兼容性；</p></li><li><p>触发频率不稳定，受很多因素影响。比如当我们的浏览器切换 tab 后，之前 tab 注册的 <code>requestIdleCallback</code> 触发的频率会变得很低。</p></li></ul><p>基于以上原因，在 React 中实现了功能更完备 <code>的requestIdleCallbackpolyfill</code>，这就是 <code>Scheduler</code>。除了在空闲时触发回调的功能外，<code>Scheduler</code> 还提供了多种调度优先级供任务设置</p><h2 id="react-fiber-中的时间分片" tabindex="-1"><a class="header-anchor" href="#react-fiber-中的时间分片" aria-hidden="true">#</a> React Fiber 中的时间分片</h2><p>把一个耗时长的任务分成很多小片，每一个小片的运行时间很短，虽然总时间依然很长，但是在每个小片执行完之后，都给其他任务一个执行的机会，这样唯一的线程就不会被独占，其他任务依然有运行的机会。</p><p>React Fiber 把更新过程碎片化，每执行完一段更新过程，就把控制权交还给 React 负责任务协调的模块，看看有没有其他紧急任务要做，如果没有就继续去更新，如果有紧急任务，那就去做紧急任务</p><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>在 React 内部采用 requestAnimationFrame 作为 ployfill，通过 帧率动态调整，计算 timeRemaining，模拟 requestIdleCallback，从而实现时间分片（Time Slicing）</p></div><h3 id="stack-reconciler" tabindex="-1"><a class="header-anchor" href="#stack-reconciler" aria-hidden="true">#</a> Stack Reconciler</h3><p>基于栈的 Reconciler，浏览器引擎会从执行栈的顶端开始执行，执行完毕就弹出当前执行上下文，开始执行下一个函数，直到执行栈被清空才会停止。然后将执行权交还给浏览器。由于 React 将页面视图视作一个个函数执行的结果。每一个页面往往由多个视图组成，这就意味着多个函数的调用。</p><p>如果一个页面足够复杂，形成的函数调用栈就会很深。每一次更新，执行栈需要一次性执行完成，中途不能干其他的事儿，只能&quot;一心一意&quot;。结合前面提到的浏览器刷新率，JS 一直执行，浏览器得不到控制权，就不能及时开始下一帧的绘制。如果这个时间超过 16ms，当页面有动画效果需求时，动画因为浏览器不能及时绘制下一帧，这时动画就会出现卡顿。不仅如此，因为事件响应代码是在每一帧开始的时候执行，如果不能及时绘制下一帧，事件响应也会延迟</p><h3 id="fiber-reconciler" tabindex="-1"><a class="header-anchor" href="#fiber-reconciler" aria-hidden="true">#</a> Fiber Reconciler</h3><p>在 React Fiber 中用链表遍历的方式替代了 React 16 之前的栈递归方案。在 React 16 中使用了大量的链表。</p><p>使用多向链表的形式替代了原来的树结构；</p><div class="language-html line-numbers-mode" data-ext="html"><pre class="language-html"><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>A<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>
A1
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>B1<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>
  B1
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>C1<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>div</span> <span class="token attr-name">id</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>B2<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>
  B2
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>div</span><span class="token punctuation">&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="`+t+'" alt=""></p><p>副作用单链表；</p><p><img src="'+p+'" alt=""></p><p>状态更新单链表；</p><p><img src="'+o+'" alt=""></p><p>链表是一种简单高效的数据结构，它在当前节点中保存着指向下一个节点的指针；遍历的时候，通过操作指针找到下一个元素。</p><p>链表相比顺序结构数据格式的好处就是：</p><ol><li><p>操作更高效，比如顺序调整、删除，只需要改变节点的指针指向就好了。</p></li><li><p>不仅可以根据当前节点找到下一个节点，在多向链表中，还可以找到他的父节点或者兄弟节点</p></li></ol><p>但链表也不是完美的，缺点就是：</p><ol><li><p>比顺序结构数据更占用空间，因为每个节点对象还保存有指向下一个对象的指针。</p></li><li><p>不能自由读取，必须找到他的上一个节点</p></li></ol><p>React 用空间换时间，更高效的操作可以方便根据优先级进行操作。同时可以根据当前节点找到其他节点，在下面提到的挂起和恢复过程中起到了关键作用</p><h3 id="react-fiber-是如何实现更新过程可控" tabindex="-1"><a class="header-anchor" href="#react-fiber-是如何实现更新过程可控" aria-hidden="true">#</a> React Fiber 是如何实现更新过程可控？</h3><p>更新过程的可控主要体现在下面几个方面：</p><ul><li><p>任务拆分</p></li><li><p>任务挂起、恢复、终止</p></li><li><p>任务具备优先级</p></li></ul><h3 id="任务拆分" tabindex="-1"><a class="header-anchor" href="#任务拆分" aria-hidden="true">#</a> 任务拆分</h3><p>在 React Fiber 机制中，它采用&quot;化整为零&quot;的思想，将调和阶段（Reconciler）递归遍历 VDOM 这个大任务分成若干小任务，每个任务只负责一个节点的处理</p><h3 id="任务挂起、恢复、终止" tabindex="-1"><a class="header-anchor" href="#任务挂起、恢复、终止" aria-hidden="true">#</a> 任务挂起、恢复、终止</h3><p><strong>workInProgress tree</strong></p><p>workInProgress 代表当前正在执行更新的 Fiber 树。在 render 或者 setState 后，会构建一颗 Fiber 树，也就是 workInProgress tree，这棵树在构建每一个节点的时候会收集当前节点的副作用，整棵树构建完成后，会形成一条完整的副作用链</p><p><strong>currentFiber tree</strong></p><p>currentFiber 表示上次渲染构建的 Filber 树。在每一次更新完成后 workInProgress 会赋值给 currentFiber。在新一轮更新时 workInProgress tree 再重新构建，新 workInProgress 的节点通过 alternate 属性和 currentFiber 的节点建立联系</p><p>在新 workInProgress tree 的创建过程中，会同 currentFiber 的对应节点进行 Diff 比较，收集副作用。同时也会复用和 currentFiber 对应的节点对象，减少新创建对象带来的开销。也就是说无论是创建还是更新、挂起、恢复以及终止操作都是发生在 workInProgress tree 创建过程中的。workInProgress tree 构建过程其实就是循环的执行任务和创建下一个任务</p><h3 id="挂起" tabindex="-1"><a class="header-anchor" href="#挂起" aria-hidden="true">#</a> 挂起</h3><p>当第一个小任务完成后，先判断这一帧是否还有空闲时间，没有就挂起下一个任务的执行，记住当前挂起的节点，让出控制权给浏览器执行更高优先级的任务</p><h3 id="恢复" tabindex="-1"><a class="header-anchor" href="#恢复" aria-hidden="true">#</a> 恢复</h3><p>在浏览器渲染完一帧后，判断当前帧是否有剩余时间，如果有就恢复执行之前挂起的任务。如果没有任务需要处理，代表调和阶段完成，可以开始进入渲染阶段。</p><p>如何判断一帧是否有空闲时间的呢？ 使用前面提到的 RIC (RequestIdleCallback) 浏览器原生 API，React 源码中为了兼容低版本的浏览器，对该方法进行了 Polyfill。</p><p>恢复执行的时候又是如何知道下一个任务是什么呢？ 答案是在前面提到的链表。在 React Fiber 中每个任务其实就是在处理一个 FiberNode 对象，然后又生成下一个任务需要处理的 FiberNode</p><h3 id="终止" tabindex="-1"><a class="header-anchor" href="#终止" aria-hidden="true">#</a> 终止</h3><p>其实并不是每次更新都会走到提交阶段。当在调和过程中触发了新的更新，在执行下一个任务的时候，判断是否有优先级更高的执行任务，如果有就终止原来将要执行的任务，开始新的 workInProgressFiber 树构建过程，开始新的更新流程。这样可以避免重复更新操作。这也是在 React 16 以后生命周期函数 componentWillMount 有可能会执行多次的原因</p><p><img src="'+i+`" alt=""></p><h2 id="任务具备优先级" tabindex="-1"><a class="header-anchor" href="#任务具备优先级" aria-hidden="true">#</a> 任务具备优先级</h2><p>React Fiber 除了通过挂起，恢复和终止来控制更新外，还给每个任务分配了优先级。具体点就是在创建或者更新 FiberNode 的时候，通过算法给每个任务分配一个到期时间（expirationTime）。在每个任务执行的时候除了判断剩余时间，如果当前处理节点已经过期，那么无论现在是否有空闲时间都必须执行该任务。过期时间的大小还代表着任务的优先级。</p><p>任务在执行过程中顺便收集了每个 FiberNode 的副作用，将有副作用的节点通过 firstEffect、lastEffect、nextEffect 形成一条副作用单链表 A1(TEXT)-B1(TEXT)-C1(TEXT)-C1-C2(TEXT)-C2-B1-B2(TEXT)-B2-A。</p><p>其实最终都是为了收集到这条副作用链表，有了它，在接下来的渲染阶段就通过遍历副作用链完成 DOM 更新。这里需要注意，更新真实 DOM 的这个动作是一气呵成的，不能中断，不然会造成视觉上的不连贯（commit）。</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token operator">&lt;</span>div id<span class="token operator">=</span><span class="token string">&quot;A1&quot;</span><span class="token operator">&gt;</span>
  <span class="token constant">A1</span>
  <span class="token operator">&lt;</span>div id<span class="token operator">=</span><span class="token string">&quot;B1&quot;</span><span class="token operator">&gt;</span>
    <span class="token constant">B1</span>
    <span class="token operator">&lt;</span>div id<span class="token operator">=</span><span class="token string">&quot;C1&quot;</span><span class="token operator">&gt;</span><span class="token constant">C1</span><span class="token operator">&lt;</span><span class="token operator">/</span>div<span class="token operator">&gt;</span>
    <span class="token operator">&lt;</span>div id<span class="token operator">=</span><span class="token string">&quot;C2&quot;</span><span class="token operator">&gt;</span><span class="token constant">C2</span><span class="token operator">&lt;</span><span class="token operator">/</span>div<span class="token operator">&gt;</span>
  <span class="token operator">&lt;</span><span class="token operator">/</span>div<span class="token operator">&gt;</span>
  <span class="token operator">&lt;</span>div id<span class="token operator">=</span><span class="token string">&quot;B2&quot;</span><span class="token operator">&gt;</span>
    <span class="token constant">B2</span>
  <span class="token operator">&lt;</span><span class="token operator">/</span>div<span class="token operator">&gt;</span>
<span class="token operator">&lt;</span><span class="token operator">/</span>div<span class="token operator">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>https://segmentfault.com/a/1190000039682751</p><h2 id="fiber-是如何工作的" tabindex="-1"><a class="header-anchor" href="#fiber-是如何工作的" aria-hidden="true">#</a> Fiber 是如何工作的</h2><p>现在我们知道了开发 fiber 的动机，让我们总结下需要实现的特性。</p><p>再一次引用 Andrew Clark’s 的笔记：</p><p>为不同类型的任务赋予优先级 任务的暂停与恢复 如果任务不再需要，可以中止 复用之前已完成的工作</p><p>回到我们的栈调和器，当 React 遍历树的时候，在执行栈中进行的。因此当接受到更新的时候，他们到达事件队列（排列好的）。只有当执行栈为空的时候，这些更新才会被处理。这恰恰是 fiber 通过近乎重新实现强大的能力的堆栈来解决的问题 - 暂停，恢复，丢弃等。</p><p>简而言之，一个fiber 就代表一个携带虚拟栈帧的任务单元。在之前的调和算法的实现中，React 创建了一个不可变得对象（React 元素）树，然后递归的遍历它们</p><p>在当前的实现中，React 创建了一个可以改变的 fiber 节点树。fiber 节点保留了组件的 state， props, 和潜在的需要渲染到的目标 DOM 元素</p><p>因为 fiber 节点是可以改变的，React 不需要每次更新都为每个节点重新创建一遍 - 它可以简单地克隆，当有更新的时候更新这个节点。另外再 fiber 树的情况下，React 不用递归遍历；相反，它创建了一个单向链表和父到子，深度优先的遍历</p><p>这里介绍一下 fiber 另外几种属性：</p><p><strong>pendingProps and memoizedProps</strong></p><p>记忆（memoization）意味着函数的执行结果，你可以后面再使用，避免重新计算。pendingProps 代表传入组件的 props，memoizedProps 在执行栈的末尾初始化，存储这个节点的 props。（译者注：pendingProps 代表 componentWillReceiveProps 的第一个参数，nextProps, memorized 代表 this.props。这里不知道对不对）</p><p>当使用到来的 pendingProps 和 memoizedProps 一样时，代表fiber 之前的输出可以复用，避免没必要的工作量。</p><p><strong>pendingWorkPriority</strong></p><p>fiber 用一个数字代表任务的优先级。ReactPriorityLevel 模块列出了不同的优先级及代表什么。有个例外就是 NoWork 是 0, 标明一个较低的优先级。</p><p><strong>Alternate</strong></p><p>任何时候，一个组件实例最多两个对应的 fiber 节点：当前的 fiber 和 处于工作中的 fiber。当前 fiber 的alternate 就是 工作中的 fiber，工作中的 alternate 就是当前的 fiber。（当前的 fiber 和 工作中的 fiber 都有一个 alternate 属性指向对方）。当前 fiber 代表已经渲染的内容，工作中的 fiber 是概念上的还没有返回值的栈帧（栈帧在执行中，还没执行完返回值）</p><p>https://zhuanlan.zhihu.com/p/98295862</p><p>最早是从Lin Clark分享的经典Fiber演讲中了解到的时间切片。时间切片指的是一种将多个粒度小的任务放入一个个时间切片中执行的一种方法</p><p><strong>时间切片的作用</strong></p><p>在刚执行完一个时间切片准备执行下一个时间切片前，React能够：</p><ul><li><p>判断是否有用户界面交互事件和其他需要执行的代码，比如点击事件，有的话则执行该事件</p></li><li><p>判断是否有优先级更高的任务需要执行，如果有，则中断当前任务，执行更高的优先级任务。也就是利用时间前片来实现高优先级任务插队。</p></li></ul><p>即时间切片有两个作用：</p><ul><li><p>在执行任务过程中，不阻塞用户与页面交互，立即响应交互事件和需要执行的代码</p></li><li><p>实现高优先级插队</p></li></ul><h2 id="react对时间切片的实现" tabindex="-1"><a class="header-anchor" href="#react对时间切片的实现" aria-hidden="true">#</a> React对时间切片的实现</h2><p>首先在这里引入当前React版本中的一段注释说明：</p><blockquote><p>// Scheduler periodically yields in case there is other work on the main<br> // thread, like user events. By default, it yields multiple times per frame.<br> // It does not attempt to align with frame boundaries, since most tasks don&#39;t<br> // need to be frame aligned; for those that do, use requestAnimationFrame.<br> let yieldInterval = 5;</p></blockquote><p><code>let yieldInterval = 5</code>，表示时间切片周期, 默认是 <code>5ms</code> (如果一个 task 运行超过该周期, 下一个 task 执行之前, 会把控制权归还浏览器)<br> 注释中说一个帧中会有多个时间切片（显而易见，一帧~=16.67ms，包含3个时间切片还多），切片时间不会与帧对齐，如果要与帧对齐，则使用 <code>requestAnimationFrame</code></p><h3 id="message-channel和fiber" tabindex="-1"><a class="header-anchor" href="#message-channel和fiber" aria-hidden="true">#</a> Message Channel和Fiber</h3><p>了解时间切片实现方法前需掌握的知识点:Message Channel 和 Fiber</p><p><strong>Message Channel</strong></p><p>浏览器提供的一种数据通信接口，可用来实现订阅发布。其特点是其两个端口属性支持双向通信和异步发布事件</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token keyword">const</span> channel <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">MessageChannel</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token keyword">const</span> port1 <span class="token operator">=</span> channel<span class="token punctuation">.</span>port1
<span class="token keyword">const</span> port2 <span class="token operator">=</span> channel<span class="token punctuation">.</span>port2

port1<span class="token punctuation">.</span><span class="token function-variable function">onmessage</span> <span class="token operator">=</span> <span class="token parameter">e</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> e<span class="token punctuation">.</span>data <span class="token punctuation">)</span> <span class="token punctuation">}</span>
port2<span class="token punctuation">.</span><span class="token function">postMessage</span><span class="token punctuation">(</span><span class="token string">&#39;from port2&#39;</span><span class="token punctuation">)</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&#39;after port2 postMessage&#39;</span> <span class="token punctuation">)</span>

port2<span class="token punctuation">.</span><span class="token function-variable function">onmessage</span> <span class="token operator">=</span> <span class="token parameter">e</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span> console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> e<span class="token punctuation">.</span>data <span class="token punctuation">)</span> <span class="token punctuation">}</span>
port1<span class="token punctuation">.</span><span class="token function">postMessage</span><span class="token punctuation">(</span><span class="token string">&#39;from port1&#39;</span><span class="token punctuation">)</span>
console<span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span> <span class="token string">&#39;after port1 postMessage&#39;</span> <span class="token punctuation">)</span>

<span class="token comment">// 控制台输出: </span>
<span class="token comment">// after port2 postMessage</span>
<span class="token comment">// after port1 postMessage</span>
<span class="token comment">// from port2</span>
<span class="token comment">// from port1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Fiber</strong></p><p>Fiber是一个的节点对象，React使用链表的形式将所有Fiber节点连接，形成链表树，即虚拟DOM树</p><p>当有更新出现，React会生成一个工作中的 Fiber树，并对工作中 Fiber树 上每一个 Fiber节点 进行计算和 <code>diff</code>，完成计算工作（React称之为渲染步骤）之后，再更新DOM（提交步骤）</p><h3 id="具体实现" tabindex="-1"><a class="header-anchor" href="#具体实现" aria-hidden="true">#</a> 具体实现</h3><p>首先 React 会默认有许多微小任务，即所有的工作中 fiber节点</p><p>在执行调度工作循环和计算工作循环时，执行每一个工作中Fiber。但是，有一个条件是每隔 <code>5毫秒</code>，会跳出工作循环，运行一次异步的 <code>MessageChannel</code> 的 <code>port.postMessage(...)</code> 方法，检查是否存在事件响应、更高优先级任务或其他代码需要执行，如果有则执行，如果没有则重新创建工作循环，执行剩下的工作中Fiber</p><p>当执行 setState() 或首次 render() 时，进入工作循环，循环体中处理的单元为 Fiber Node, 即是拆分任务的最小单位，从根节点开始，自顶向下逐节点构造 workInProgress tree（构建中的新 Fiber Tree）。 beginWork() 主要做的事情是从顶向下生成所有的 Fiber Node，并标记 Diff。 completeUnitOfWork() 当没有子节点，开始遍历兄弟节点作为下一个处理单元，处理完兄弟节点开始向上回溯，真到再次回去根节点为止，将收集向上回溯过程中的所有 diff，拿到 diff 后开始进入 commit 阶段。 构建 workInProgress tree 的过程就是 diff 的过程，通过 requestIdleCallback 来调度执行一组任务，每完成一个任务后回来看看有没有插队的（更紧急的），把时间控制权交还给主线程，直到下一次 requestIdleCallback 回调再继续构建 workInProgress tree。</p><p>https://juejin.cn/post/6844904081178230792</p><p>https://zhuanlan.zhihu.com/p/60307571</p>`,104),l=[r];function u(d,k){return s(),a("div",null,l)}const b=n(c,[["render",u],["__file","时间切片.html.vue"]]);export{b as default};
