import{_ as e,o as n,c as a,e as o}from"./app-NJfECqZf.js";const t={},s=o(`<h1 id="安全区域" tabindex="-1"><a class="header-anchor" href="#安全区域" aria-hidden="true">#</a> 安全区域</h1><p>在iPhoneX发布后，许多厂商相继推出了具有边缘屏幕的手机</p><p>这些手机和普通手机在外观上无外乎做了三个改动：圆角（corners）、刘海（sensor housing）和小黑条（Home Indicator）。为了适配这些手机，安全区域这个概念变诞生了：安全区域就是一个不受上面三个效果的可视窗口范围。</p><p>为了保证页面的显示效果，我们必须把页面限制在安全范围内</p><p>下面以 iphoneX 为例</p><h2 id="viewport-fit" tabindex="-1"><a class="header-anchor" href="#viewport-fit" aria-hidden="true">#</a> viewport-fit</h2><p><code>viewport-fit</code> 是专门为了适配 iPhoneX 而诞生的一个属性，它用于限制网页如何在安全区域内进行展示</p><p><code>&lt;meta name=&quot;viewport&quot; content=&quot;viewport-fit=cover&quot;&gt;</code></p><p><code>viewport-fit</code> 的值：</p><ul><li><p><code>auto</code>: 此值不影响初始布局视图端口，并且整个web页面都是可查看的</p></li><li><p><code>contain</code>: 视图端口按比例缩放，以适合显示内嵌的最大矩形</p></li><li><p><code>cover</code>：视图端口被缩放以填充设备显示。强烈建议使用 <code>safe area inset</code> 变量，以确保重要内容不会出现在显示之外</p></li></ul><h3 id="env、constant" tabindex="-1"><a class="header-anchor" href="#env、constant" aria-hidden="true">#</a> env、constant</h3><p><code>env()</code>和 <code>constant()</code> CSS 函数以类似于 <code>var</code> 函数将用户代理定义的环境变量值插入你的 CSS 中</p><p>如 <code>env(safe-area-inset-bottom)</code>表示获取安全区域距离底部边界距离</p><p><code>safe-area-inset-[]</code>可以是四个常量：</p><ul><li><p><code>safe-area-inset-left</code>：安全区域距离左边边界距离</p></li><li><p><code>safe-area-inset-right</code>：安全区域距离右边边界距离</p></li><li><p><code>safe-area-inset-top</code>：安全区域距离顶部边界距离</p></li><li><p><code>safe-area-inset-bottom</code>：安全区域距离底部边界距离</p></li></ul><p><code>constant</code> 在 <code>iOS &lt; 11.2</code> 的版本中生效，<code>env</code> 在 <code>iOS &gt;= 11.2</code> 的版本中生效，这意味着我们往往要同时设置他们，将页面限制在安全区域内：</p><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token selector">body</span> <span class="token punctuation">{</span>
  <span class="token property">padding-bottom</span><span class="token punctuation">:</span> <span class="token function">constant</span><span class="token punctuation">(</span>safe-area-inset-bottom<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token property">padding-bottom</span><span class="token punctuation">:</span> <span class="token function">env</span><span class="token punctuation">(</span>safe-area-inset-bottom<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>上面样式规则表示排除了底部不可用的一块区域</p>`,18),c=[s];function i(p,d){return n(),a("div",null,c)}const l=e(t,[["render",i],["__file","适配iponeX.html.vue"]]);export{l as default};
